package com.webcommander.plugin.popup.constants

class Constants {

    static CONTENT_TYPE = [
        html: "html"
    ]

    static FREQUENCY = [
        ALWAYS: "always",
        ONE_TIME: "one_time"
    ]

    static FREQUENCY_NAMES = [
        (FREQUENCY.ALWAYS): "always",
        (FREQUENCY.ONE_TIME): "one.time"
    ]
}
