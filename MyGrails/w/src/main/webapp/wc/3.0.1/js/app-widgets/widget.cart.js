app.widget.cart = function(config) {
    app.widget.cart._super.constructor.apply(this, arguments);
}

var _c = app.widget.cart.inherit(app.widget.base);

_c.init = function() {
    app.widget.cart._super.init.call(this);
}
